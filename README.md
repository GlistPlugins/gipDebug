# gipDebug
This repository contains the binary files for gdb, which are required to debug on Windows.

- Windows developers should not forget to add;
```
${workspace_loc}\..\..\..\..\glistplugins\gipDebug\libs\bin
```
directory to the GlistApp project's PATH list.
(Project->Properties->C/C++ Build->Environment->PATH)
